import React from "react";

const RequestLayout = ({ children }: { children: React.ReactNode }) => {
  return <section className="px-6 pb-20 pt-6">{children}</section>;
};

export default RequestLayout;
